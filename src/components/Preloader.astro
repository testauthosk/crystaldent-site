---
/**
 * Прелоадер-метафора: чёрные зубы становятся белыми.
 *
 * Старт — чёрный экран (сетка чёрных кубиков на белой подложке).
 * Волна идёт от четырёх углов к центру: фронт вспыхивает белой окантовкой,
 * кубик схлопывается, за ним остаётся белое. Чернота «отбеливается».
 * В центре последним остаётся чёрный знак-зуб — и он тоже отбеливается,
 * снизу вверх, растворяясь в белом. Затем подложка уходит вверх.
 *
 * Никаких градиентов: у кубика два состояния — чёрный и белый.
 */
---

<div id="preloader" aria-hidden="true">
  <div class="pre-mark" id="preMark">
    <span class="mark-dark"></span>
    <span class="mark-lit" id="preMarkLit"></span>
  </div>
  <div id="preTiles" class="tiles"></div>
</div>

<script is:inline>
  // Сетку строим синхронно, до первой отрисовки: если ждать модуль motion.ts,
  // экран успевает моргнуть белым.
  (() => {
    if (matchMedia("(prefers-reduced-motion: reduce)").matches) return;
    const grid = document.getElementById("preTiles");
    if (!grid) return;

    const vw = window.innerWidth;
    const tile = vw >= 1024 ? 68 : vw >= 640 ? 48 : 32; // на компе крупнее, на мобиле мельче
    const cols = Math.ceil(vw / tile);
    const rows = Math.ceil(window.innerHeight / tile);

    grid.style.gridTemplateColumns = `repeat(${cols}, 1fr)`;
    grid.style.gridTemplateRows = `repeat(${rows}, 1fr)`;
    grid.dataset.cols = String(cols);
    grid.dataset.rows = String(rows);

    const frag = document.createDocumentFragment();
    for (let i = 0; i < rows * cols; i++) {
      const d = document.createElement("div");
      d.className = "tile";
      frag.appendChild(d);
    }
    grid.appendChild(frag);
  })();
</script>

<style>
  #preloader {
    position: fixed;
    inset: 0;
    z-index: 100;
    pointer-events: none;
    background: var(--color-paper);
  }
  html:not(.js) #preloader {
    display: none;
  }

  .tiles {
    position: absolute;
    inset: 0;
    z-index: 1;
    display: grid;
  }

  /* в покое экран читается чёрным: окантовка едва темнее заливки.
     белой она вспыхивает только на гребне волны */
  .tiles :global(.tile) {
    box-sizing: border-box;
    background: #0b1116;
    border: 1px solid #16212a;
    will-change: transform, border-color;
  }

  /* знак под сеткой: последний островок черноты */
  .pre-mark {
    position: absolute;
    z-index: 0;
    top: 50%;
    left: 50%;
    width: clamp(54px, 7vw, 98px);
    aspect-ratio: 124 / 152;
    transform: translate(-50%, -50%);
  }
  .pre-mark span {
    position: absolute;
    inset: 0;
    -webkit-mask: url("/brand/mark.svg") center / contain no-repeat;
    mask: url("/brand/mark.svg") center / contain no-repeat;
  }
  .mark-dark {
    background: #0b1116;
  }
  /* белый слой поверх: «отбеливание» идёт снизу вверх */
  .mark-lit {
    background: var(--color-paper);
    clip-path: inset(100% 0 0 0);
    will-change: clip-path;
  }

  @media (prefers-reduced-motion: reduce) {
    #preloader {
      display: none;
    }
  }
</style>
